Fix these problems in the documentations
* The Google SSO credentials are mentioned in the .envrc.example but they need to be inserted manually in PocketBase Admin panel.
* In prod, it is not true that PUBLIC_PB_URL can be '', otherwise the Google SSO will not work because Astro will generate URL like /login/api instead of just /api.
* Make sure that Litestream environment variables are explained
* Make sure that SUPERUSER_EMAIL and SUPERUSER_PASSWORD are explained
