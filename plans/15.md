Refactor the deployment to make the pocketbase serve invocation explicit.
All dirs should be passed like below
./pocketbase serve \
  --dir="./custom_data" \
  --publicDir="./custom_public" \
  --hooksDir="./custom_hooks" \
  --migrationsDir="./custom_migrations"

